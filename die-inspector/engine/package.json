{
  "name": "die-engine",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "DIE Inspector evaluation engine — automated rubric audits via typesafe-ai/jev on the Vercel AI Gateway.",
  "scripts": {
    "test": "tsx die-test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "ai": "^7.0.107"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
